{
  "skill_name": "sap-theming",
  "evals": [
    {
      "id": 1,
      "prompt": "I have a standalone SAPUI5 app running on our internal server and I need to apply our custom theme acme_dark to it. The UI Theme Designer runs at https://theming.acme.corp. How do I configure my app to use this theme?",
      "expected_output": "Step-by-step instructions covering the theme origin allowlist meta tag, at least one of the three integration methods (URL params, bootstrap attrs, global config) with the correct theme root, the /UI5 suffix requirement, and versionedLibCss=true.",
      "files": [],
      "assertions": [
        {"description": "Mentions sap-allowed-theme-origins meta tag", "check": "Response mentions sap-allowed-theme-origins as a required meta tag for the theme origin allowlist"},
        {"description": "Includes correct integration method with code", "check": "Response shows at least one correct method (URL params with sap-theme=, bootstrap data-sap-ui-theme, or globalThis sap-ui-config) with code example"},
        {"description": "Mentions /UI5 suffix requirement", "check": "Response explains that /UI5 must be appended to the theme root when using bootstrap attributes or global configuration"},
        {"description": "Mentions versionedLibCss", "check": "Response includes versionedLibCss=true as a required configuration"},
        {"description": "Uses correct theme root URL", "check": "Response uses https://theming.acme.corp (from the prompt) in its examples, not a generic placeholder"}
      ]
    },
    {
      "id": 2,
      "prompt": "We've defined palette parameters in our custom theme for our brand colors (acmeBlue, acmeGreen). Now we want to use them in the CSS of our own custom SAPUI5 control com.acme.StatusBadge. The control has its own style.css bundled with the control library. How can we reference the theme colors in that CSS without hardcoding hex values?",
      "expected_output": "Explains the CSS custom property bridge pattern: add custom CSS in the theme editor that exposes palette parameters as CSS custom properties via :root { --acmeBlue: @acmeBlue; }, then use var(--acmeBlue, fallback) in the control's style.css.",
      "files": [],
      "assertions": [
        {"description": "Shows :root CSS custom property bridge pattern", "check": "Response includes a code example with :root { --acmeBlue: @acmeBlue; } or similar pattern exposing palette params as CSS custom properties"},
        {"description": "Explains custom CSS is LESS", "check": "Response mentions that the custom CSS editor uses LESS (specifically LESS 1.6.3 or just LESS), which is why @variable syntax works"},
        {"description": "Shows var() usage in control CSS", "check": "Response shows using var(--acmeBlue) or var(--acmeBlue, fallback) in the control's style.css"},
        {"description": "Mentions per-UI-technology custom CSS", "check": "Response mentions that custom CSS must be added for the SAPUI5 UI technology specifically, or at least mentions selecting the right UI technology"}
      ]
    },
    {
      "id": 3,
      "prompt": "Our custom theme is based on sap_belize_hcb and we just upgraded our app to SAPUI5 1.136. The app now shows a completely different theme than what we designed. What's happening and how do we fix it?",
      "expected_output": "Explains that Belize themes were removed in recent SAPUI5 versions, describes the theme fallback mechanism (the 5 phases), explains that the theme likely fell back to sap_horizon_hcb via the flavor fallback chain, and recommends migrating the custom theme to be based on a Horizon theme.",
      "files": [],
      "assertions": [
        {"description": "Explains Belize deprecation", "check": "Response explains that Belize themes (sap_belize_hcb) were removed or are no longer supported in recent SAPUI5 versions like 1.136"},
        {"description": "Describes theme fallback mechanism", "check": "Response describes the theme fallback mechanism — the multi-phase process the theming service uses when a theme is unavailable"},
        {"description": "Identifies likely fallback result", "check": "Response identifies that the theme likely fell back to a Horizon variant (sap_horizon_hcb) or another HCB theme via the flavor fallback"},
        {"description": "Recommends migration to Horizon", "check": "Response recommends migrating the custom theme to be based on a Horizon or Quartz theme as the fix"}
      ]
    },
    {
      "id": 4,
      "prompt": "I'm building a custom tile component for our SAP Fiori app. Generate the CSS classes for the tile using SAP theming parameters so it looks consistent with the standard SAP Fiori tiles. The tile has a default state, hover state, active state, a title, and a subtitle. It should also have a border and rounded corners.",
      "expected_output": "Generates CSS using var(--sapTile_*) CSS custom properties for background, border, border-radius, text colors, hover, and active states. Uses real SAP parameter names (not invented ones). Does NOT use hardcoded fallback values in var() — uses bare var(--sapXxx) instead. Uses rem for dimensions. Mentions the Parameter Explorer or parameter dependencies doc as a way to look up additional parameters. Mentions that the developer must ensure SAP theme CSS custom properties are available at runtime.",
      "files": [],
      "assertions": [
        {"description": "Uses --sapTile_Background CSS custom property", "check": "Response uses var(--sapTile_Background) for the tile background color"},
        {"description": "Uses --sapTile_BorderColor CSS custom property", "check": "Response uses var(--sapTile_BorderColor) for the tile border color"},
        {"description": "Covers hover and active states with correct parameters", "check": "Response includes hover state using --sapTile_Hover_Background and/or active state using --sapTile_Active_Background"},
        {"description": "Does NOT use hardcoded fallback values in var()", "check": "Response uses bare var(--sapXxx) without hardcoded fallback values (NOT var(--sapXxx, #fff) or similar). It may mention that the developer must ensure the theme CSS is loaded instead."},
        {"description": "Uses rem for dimensions", "check": "Response uses rem units (not px) for dimension values like border-radius, padding, font-size, or spacing"},
        {"description": "References Parameter Explorer or parameter dependencies doc", "check": "Response mentions the Parameter Explorer (sap.github.io/theming-base-content) or the parameter dependencies documentation as a resource for looking up additional parameters"}
      ]
    },
    {
      "id": 5,
      "prompt": "I have a button component CSS file that uses hardcoded colors and our old design system's custom properties. Please migrate it to use SAP's design system CSS custom properties so it works with SAP theming (dark mode, high contrast, corporate branding). The file is at evals/button_premigration.css.",
      "expected_output": "Migrated CSS replacing hardcoded values and legacy custom properties with var(--sapButton_*) and generic SAP parameters. Maps .btn-success to sapButton_Success_* (not Accept). Uses sapContent_DisabledOpacity for disabled opacity. Uses sapContent_FocusColor/FocusWidth/FocusStyle for focus outlines. Removes or annotates the :root block. Lists unmatched and unused properties.",
      "files": ["evals/button_premigration.css"],
      "assertions": [
        {"description": "Maps .btn-success to sapButton_Success_*, not sapButton_Accept_*", "check": "Response maps .btn-success to --sapButton_Success_Background (or other sapButton_Success_* parameters), NOT to sapButton_Accept_* parameters"},
        {"description": "Uses sapContent_DisabledOpacity for disabled button opacity", "check": "Response uses var(--sapContent_DisabledOpacity) for the disabled state opacity instead of the hardcoded 0.5"},
        {"description": "Uses sapContent_Focus* for focus outline", "check": "Response uses var(--sapContent_FocusColor) and/or var(--sapContent_FocusWidth) for the focus/focus-visible outline instead of the hardcoded 2px solid #1A00EB"},
        {"description": "Maps .btn-danger to sapButton_Negative_*, not sapButton_Reject_*", "check": "Response maps .btn-danger to --sapButton_Negative_Background (or other sapButton_Negative_* parameters), NOT to sapButton_Reject_* parameters"},
        {"description": "Removes or annotates the legacy :root block", "check": "Response removes the :root block with legacy custom properties or adds a comment explaining that SAP theming infrastructure provides these values"},
        {"description": "Uses bare var() without hardcoded fallback values", "check": "Response uses bare var(--sapXxx) without hardcoded fallback values (NOT var(--sapXxx, #fff) or similar)"},
        {"description": "Uses variant-specific hover/active parameters", "check": "For emphasized/success/negative/lite button variants, hover and active states use the variant-specific parameters (e.g. sapButton_Emphasized_Hover_Background, sapButton_Lite_Active_Background) rather than the base sapButton_Hover_* or sapButton_Active_*"},
        {"description": "Does not preserve hardcoded transparent for .btn-secondary background", "check": "For .btn-secondary, response uses var(--sapButton_Background) or another SAP parameter for background-color, NOT the hardcoded value 'transparent' from the original CSS"}
      ]
    },
    {
      "id": 6,
      "prompt": "Migrate this button CSS to SAP design system parameters: evals/button_premigration.css. After the migration, list any legacy custom properties that couldn't be mapped to an SAP equivalent, and list SAP button parameters that are relevant but not used in the migrated CSS.",
      "expected_output": "Migrated CSS plus two lists: (1) unmatched legacy properties including --button-border-style which has no SAP equivalent, and (2) unused SAP button parameters that could be added for more comprehensive theming.",
      "files": ["evals/button_premigration.css"],
      "assertions": [
        {"description": "Identifies --button-border-style as unmatched", "check": "Response identifies --button-border-style as a legacy custom property that has no SAP equivalent"},
        {"description": "Lists unused SAP button parameters", "check": "Response lists at least 2 SAP button parameters that are relevant to buttons but not used in the migrated CSS"},
        {"description": "Maps .button-primary to sapButton_Emphasized_*", "check": "Response maps .button-primary to --sapButton_Emphasized_Background or other sapButton_Emphasized_* parameters"},
        {"description": "Uses complete parameter sets per variant (component-first)", "check": "For at least one variant (e.g. emphasized), the migration includes parameters beyond what existed in the original CSS, such as font-family, text-shadow, or font-weight specific to that variant — showing a component-first approach rather than 1:1 property replacement"},
        {"description": "Summarizes unused parameters by family, not individual params", "check": "The unused SAP parameters section groups or summarizes parameters by family/namespace (e.g. 'sapButton_Accept_*') rather than listing every individual parameter one by one"}
      ]
    },
    {
      "id": 7,
      "prompt": "I need to migrate my button CSS to SAP theming parameters. My CSS has these button variants: .btn (default), .button-primary (main CTA), .btn-success (positive feedback state), .btn-danger (error/destructive), .btn-warning (caution), .btn-link (transparent/text-only). Which SAP button parameter families should each variant map to? Explain the reasoning, especially for any non-obvious mappings.",
      "expected_output": "Mapping table with correct semantic assignments: default -> sapButton_, primary -> sapButton_Emphasized_, success -> sapButton_Success_ (NOT Accept), danger -> sapButton_Negative_, warning -> sapButton_Critical_, link -> sapButton_Lite_. Explains the Accept vs Success distinction.",
      "files": [],
      "assertions": [
        {"description": "Maps primary to Emphasized", "check": "Response maps .button-primary (main CTA) to sapButton_Emphasized_* parameters"},
        {"description": "Maps success to Success, not Accept", "check": "Response maps .btn-success (positive feedback state) to sapButton_Success_* parameters and does NOT map it to sapButton_Accept_*"},
        {"description": "Maps danger to Negative, not Reject", "check": "Response maps .btn-danger (error/destructive) to sapButton_Negative_* parameters and does NOT map it to sapButton_Reject_*"},
        {"description": "Maps warning to Critical", "check": "Response maps .btn-warning (caution) to sapButton_Critical_* parameters"},
        {"description": "Maps link to Lite", "check": "Response maps .btn-link (transparent/text-only) to sapButton_Lite_* parameters"},
        {"description": "Explains Accept vs Success distinction", "check": "Response explicitly explains the difference between Accept (action: confirming/accepting) and Success (state: positive feedback) in SAP button semantics"},
        {"description": "Explains Warning vs Critical follows the same pattern", "check": "Response explains that Warning (action) vs Critical (state) follows the same action/state distinction as Accept/Success and Reject/Negative"}
      ]
    },
    {
      "id": 8,
      "prompt": "Please migrate this card component CSS to SAP theming parameters so it works with dark mode and high contrast. The file is at evals/card_premigration.css.",
      "expected_output": "Migrated CSS replacing hardcoded values with var(--sapTile_*) and generic SAP parameters. SAP uses sapTile_* for both tiles and cards (no sapCard_* family). Uses sapContent_Shadow* for box-shadow. Uses sapContent_DisabledOpacity for disabled. Removes or annotates the :root block. Lists unmatched and unused properties.",
      "files": ["evals/card_premigration.css"],
      "assertions": [
        {"description": "Uses sapTile_Background for card background", "check": "Response uses var(--sapTile_Background) for the card background color (SAP uses sapTile_* for both tiles and cards — there is no sapCard_* family)"},
        {"description": "Uses sapTile_BorderColor for card border", "check": "Response uses var(--sapTile_BorderColor) for the card border color"},
        {"description": "Uses sapContent_Shadow* for box-shadow", "check": "Response uses var(--sapContent_Shadow0) or var(--sapContent_Shadow1) or another sapContent_Shadow* parameter for the card box-shadow instead of the hardcoded rgba shadow"},
        {"description": "Uses sapTile_SeparatorColor for header/footer borders", "check": "Response uses var(--sapTile_SeparatorColor) for the internal header/footer border colors"},
        {"description": "Uses sapFontFamily for font-family", "check": "Response uses var(--sapFontFamily) or var(--sapFontHeaderFamily) instead of the hardcoded 'Segoe UI' font stack"},
        {"description": "Removes or annotates the legacy :root block", "check": "Response removes the :root block with legacy custom properties or adds a comment explaining that SAP theming provides these values"},
        {"description": "Uses bare var() without hardcoded fallback values", "check": "Response uses bare var(--sapXxx) without hardcoded fallback values"},
        {"description": "Uses sapContent_DisabledOpacity for disabled card", "check": "Response uses var(--sapContent_DisabledOpacity) for the .card.disabled opacity instead of the hardcoded 0.5"}
      ]
    },
    {
      "id": 9,
      "prompt": "I have a form field CSS file with input styles, value states (error, warning, success), disabled and read-only states. Migrate it to use SAP theming parameters. The file is at evals/field_premigration.css.",
      "expected_output": "Migrated CSS replacing hardcoded values with var(--sapField_*) and generic SAP parameters. Maps value states to sapField_Error_*, sapField_Warning_*, sapField_Success_*. Uses sapContent_DisabledOpacity and sapContent_FocusColor. Removes or annotates the :root block.",
      "files": ["evals/field_premigration.css"],
      "assertions": [
        {"description": "Uses sapField_Background for field background", "check": "Response uses var(--sapField_Background) for the base input field background color"},
        {"description": "Uses sapField_BorderColor for field border", "check": "Response uses var(--sapField_BorderColor) for the base input field border color"},
        {"description": "Maps .is-error to sapField error parameters", "check": "Response maps .is-error to sapField error parameters like var(--sapField_InvalidColor) or var(--sapField_InvalidBackground) or similar sapField_* error parameters"},
        {"description": "Maps .is-warning to sapField warning parameters", "check": "Response maps .is-warning to sapField warning parameters like var(--sapField_WarningColor) or var(--sapField_WarningBackground) or similar sapField_* warning parameters"},
        {"description": "Maps .is-success to sapField success parameters", "check": "Response maps .is-success to sapField success parameters like var(--sapField_SuccessColor) or var(--sapField_SuccessBackground) or similar sapField_* success parameters"},
        {"description": "Uses sapContent_DisabledOpacity for disabled field", "check": "Response uses var(--sapContent_DisabledOpacity) for the disabled state opacity instead of the hardcoded 0.6"},
        {"description": "Uses sapFontFamily for font-family", "check": "Response uses var(--sapFontFamily) instead of the hardcoded 'Arial' font"},
        {"description": "Removes or annotates the legacy :root block", "check": "Response removes the :root block with legacy custom properties or adds a comment explaining that SAP theming provides these values"},
        {"description": "Uses sapField_Hover_BorderColor for hover state", "check": "Response uses var(--sapField_Hover_BorderColor) or similar hover parameter for the field hover state border color"}
      ]
    },
    {
      "id": 10,
      "prompt": "Are SAP theming parameters the same thing as design tokens? I keep seeing both terms used. How does SAP's design token system work and where do the theming parameters fit in?",
      "expected_output": "Explains that SAP Design System design tokens map directly to theming parameters — they are essentially the same concept. Theming parameters are the implementation of design tokens within SAP's theming infrastructure. Mentions that the theming content is the implementation of the SAP Fiori for Web Design System. May mention LESS as the current technology with evolution toward CSS custom properties.",
      "files": [],
      "assertions": [
        {"description": "Explains design tokens map to theming parameters", "check": "Response explains that SAP Design System design tokens are mapped directly to theming parameters, or that they are essentially the same concept / two names for the same thing"},
        {"description": "Mentions theming content implements the SAP Design System", "check": "Response mentions that the theming content is the implementation of the SAP Fiori for Web Design System, or words to that effect"},
        {"description": "Mentions LESS and/or CSS custom properties as technology", "check": "Response mentions LESS as the current technology for theming parameters and/or that the system is evolving toward CSS custom properties"},
        {"description": "References the Parameter Explorer or parameter dependencies doc", "check": "Response mentions the Parameter Explorer (sap.github.io/theming-base-content) or the parameter dependencies documentation as a way to explore the available parameters"}
      ]
    },
    {
      "id": 11,
      "prompt": "I changed sapBrandColor in my Evening Horizon-based custom theme to our corporate blue. But it seems like changing this one parameter affected a whole bunch of other things in the UI — links, highlights, selected states all changed. How does this work? Which parameters are affected when I change sapBrandColor in Evening Horizon?",
      "expected_output": "Explains the parameter dependency hierarchy — sapBrandColor is a top-level parameter that other parameters derive from. In Evening Horizon specifically, sapHighlightColor depends on sapPrimary2 (not sapBrandColor), but sapActiveColor, sapLinkColor, and many component parameters depend on sapBrandColor. Should reference the parameter dependencies table and explain how to trace impact. May mention that dependencies differ between themes.",
      "files": [],
      "assertions": [
        {"description": "Explains parameter hierarchy / dependency concept", "check": "Response explains that theming parameters are organized in a hierarchy where more specific parameters derive/depend on more generic ones, so changing a top-level parameter cascades to dependent parameters"},
        {"description": "Identifies specific parameters that depend on sapBrandColor", "check": "Response identifies at least 2 specific parameters that depend on sapBrandColor (e.g. sapActiveColor, sapLinkColor, sapShellColor, sapSelectedColor, or component-level parameters)"},
        {"description": "References the parameter dependencies table", "check": "Response references the theme parameter dependencies documentation or table as the place to trace which parameters depend on which"},
        {"description": "Mentions dependencies vary between themes", "check": "Response mentions that parameter dependencies can differ between themes (e.g. Morning Horizon vs Evening Horizon vs High Contrast) — the same parameter may have different dependencies in different themes"}
      ]
    },
    {
      "id": 12,
      "prompt": "I see parameters like sapPrimary2, sapPrimary3 in the theme parameter dependencies. What are these? Is there some kind of underlying palette that all the semantic parameters get their values from? How does the whole parameter structure work from top to bottom?",
      "expected_output": "Explains that sapPrimary1-7 (and sapAccentColor1-10 etc.) form a reference palette — they are the lowest-level color constants that many semantic parameters derive from. The hierarchy goes: palette (sapPrimary*) → semantic parameters (sapBrandColor, sapBaseColor, sapHighlightColor) → component parameters (sapButton_*, sapTile_*, etc.). In some themes, sapBrandColor depends on sapPrimary2. The palette values differ between themes (Morning Horizon, Evening Horizon, etc.).",
      "files": [],
      "assertions": [
        {"description": "Identifies sapPrimary* as palette/reference colors", "check": "Response identifies sapPrimary1 through sapPrimary7 (or at least sapPrimary2/sapPrimary3) as base palette or reference colors that serve as the foundation for derived parameters"},
        {"description": "Describes the hierarchical structure", "check": "Response describes a multi-level hierarchy: base palette → semantic/generic parameters → component-specific parameters (or similar layered structure)"},
        {"description": "Explains that sapBrandColor depends on sapPrimary2", "check": "Response mentions that sapBrandColor depends on or derives from sapPrimary2 (at least in some themes like Morning Horizon or Evening Horizon)"},
        {"description": "Mentions that empty cells in the dependency table mean constants", "check": "Response explains that when a parameter has no dependency (empty cell in the table), its value is either a constant (like a hex color) or depends on a non-themable parameter"},
        {"description": "References Parameter Explorer for interactive exploration", "check": "Response mentions the Parameter Explorer (sap.github.io/theming-base-content) as a way to interactively explore the parameter hierarchy"}
      ]
    }
  ]
}
